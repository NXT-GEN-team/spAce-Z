<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>EPIC - Earth Polychromatic Imaging Camera</title>
  <link rel="stylesheet" href="/homepage.css" />
  <style>
    .epic-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }
    .epic-content {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border-radius: 15px;
      padding: 30px;
      margin: 20px 0;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    .epic-image {
      max-width: 100%;
      height: auto;
      border-radius: 10px;
      margin: 20px 0;
      box-shadow: 0 0 20px rgba(0, 208, 255, 0.3);
    }
    .epic-title {
      color: #fff;
      font-size: 2rem;
      margin-bottom: 20px;
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
    }
    .epic-caption {
      color: #ccc;
      line-height: 1.6;
      margin: 20px 0;
      font-size: 1.1rem;
    }
    .epic-meta {
      color: #999;
      font-size: 0.9rem;
      margin-top: 20px;
    }
    .error-message {
      color: #ff6b6b;
      text-align: center;
      padding: 20px;
      background: rgba(255, 107, 107, 0.1);
      border-radius: 10px;
      margin: 20px 0;
    }
    .message {
      color: #4CAF50;
      text-align: center;
      padding: 20px;
      background: rgba(76, 175, 80, 0.1);
      border-radius: 10px;
      margin: 20px 0;
    }
    .epic-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }
    .epic-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 10px;
      padding: 15px;
      transition: transform 0.3s ease;
    }
    .epic-card:hover {
      transform: translateY(-5px);
    }
  </style>
</head>
<body>
  <header>
    <div class="title">
      <h1>EPIC - Earth Polychromatic Imaging Camera</h1>
      <p>Daily images of Earth from NASA's DSCOVR satellite</p>
    </div>
  </header>

  <div class="epic-container">
    <% if (error) { %>
      <div class="error-message">
        <h2>Error</h2>
        <p><%= error %></p>
      </div>
    <% } else if (message) { %>
      <div class="message">
        <p><%= message %></p>
      </div>
    <% } else if (images && images.length > 0) { %>
      <div class="epic-grid">
        <% images.forEach((image, index) => { %>
          <div class="epic-card">
            <h3 class="epic-title">Earth View <%= index + 1 %></h3>
            <img src="<%= image.url %>" alt="EPIC Earth Image <%= index + 1 %>" class="epic-image" />
            <div class="epic-caption">
              <%= image.caption || `Earth as seen from DSCOVR satellite on ${new Date(image.date).toLocaleDateString()}` %>
            </div>
            <div class="epic-meta">
              <p><strong>Date:</strong> <%= new Date(image.date).toLocaleDateString() %></p>
              <p><strong>Time:</strong> <%= new Date(image.date).toLocaleTimeString() %></p>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="message">
        <p>No EPIC images available for the selected date.</p>
      </div>
    <% } %>
  </div>

  <!-- Load Three.js first -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
  <!-- Then load our starfield script -->
  <script src="/starfield.js"></script>
</body>
</html>
