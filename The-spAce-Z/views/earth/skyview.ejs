<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>🌌 NASA SkyView Explorer</title>
  <link rel="stylesheet" href="/homepage.css">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #000;
      color: #eee;
      padding: 2rem;
      text-align: center;
    }
    header {
      margin-bottom: 2rem;
    }
    form {
      background: #111;
      padding: 1rem 2rem;
      border-radius: 12px;
      display: inline-block;
    }
    input, select, button {
      margin: 0.5rem;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      border: none;
      font-size: 1rem;
    }
    button {
      background-color: #00ff85;
      color: #000;
      cursor: pointer;
      font-weight: bold;
    }
    img {
      max-width: 100%;
      border: 4px solid #00ff85;
      margin-top: 2rem;
      border-radius: 16px;
    }
    .error {
      color: red;
      font-weight: bold;
    }
    a {
      color: #00ff85;
      text-decoration: none;
      display: inline-block;
      margin-top: 1rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>🌌 NASA SkyView Image Explorer</h1>
    <a href="/earth">← Back to Earth Tools</a>
  </header>

  <form method="GET" action="/earth/skyview">
    <input type="text" name="target" placeholder="Object (e.g., M51, Orion)" required value="<%= target %>">
    <select name="survey">
      <option value="DSS" <%= survey === 'DSS' ? 'selected' : '' %>>DSS (Optical)</option>
      <option value="2MASS-K" <%= survey === '2MASS-K' ? 'selected' : '' %>>2MASS-K (Infrared)</option>
      <option value="IRAS-12" <%= survey === 'IRAS-12' ? 'selected' : '' %>>IRAS-12 (Infrared)</option>
      <option value="GALEXGR6-AIS" <%= survey === 'GALEXGR6-AIS' ? 'selected' : '' %>>GALEX (UV)</option>
    </select>
    <input type="number" name="pixels" placeholder="Size in pixels" min="100" max="1000" value="<%= pixels %>">
    <button type="submit">View Image</button>
  </form>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } else if (imageUrl) { %>
    <h2>🖼️ SkyView Image Result</h2>
    <img src="<%= imageUrl %>" alt="SkyView image of <%= target %>">
  <% } else { %>
    <p>Enter a celestial object and survey to view an image from NASA SkyView.</p>
  <% } %>
</body>
</html>
